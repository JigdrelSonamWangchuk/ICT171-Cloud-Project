#  Commands to Configure Ubuntu Firewall (UFW)

## Check firewall:
```bash
sudo ufw status
```

## Allow Ngnix:
```bash
sudo ufw allow 'Nginx Full'
```

## Enable firewall if disabled:
```bash
sudo ufw enable
```
